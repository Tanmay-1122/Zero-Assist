# Notice

Zero-Assist is maintained as its own Android app. The repository currently includes an upstream Rust engine submodule under `zeroclaw/` and an Android FFI workspace under `zeroclaw-android/`.

Some internal package names and native library names are retained for compatibility. Public app branding is Zero-Assist.
